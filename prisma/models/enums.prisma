enum VRChatAccountType {
  MAIN
  ALT
  IN_VERIFICATION
}

enum LeaveOfAbsenceStatus {
  PENDING
  APPROVED
  DENIED
  ACTIVE
  ENDED_EARLY
  EXPIRED
}

enum LeaveOfAbsenceType {
  ATTENDABLE
  BLOCKING
}

enum ModCaseType {
  WARN
  KICK
  BAN
  UNBAN
  TIMEOUT
  UNTIMEOUT
  SOFTBAN
  PURGE
  FILTER
  LOCK
  UNLOCK
  NOTE
}
