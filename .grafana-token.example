# Service account token for scripts/grafana-api.mjs (Bearer auth to Grafana HTTP API).
# Create at https://metrics.vrcshield.com -> Administration -> Service accounts
# Put the glsa_... token on its own line below (comments are ignored).
